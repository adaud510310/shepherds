# 📦 دليل البناء والنشر السريع

## 🚀 خطوات سريعة للنشر

### 1. بناء المشروع

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على الملفات الجاهزة.

---

## 📂 محتويات مجلد dist

بعد تشغيل `npm run build`، ستجد:

```
dist/
├── index.html              # الملف الرئيسي
├── assets/
│   ├── index-[hash].css   # ملف CSS المضغوط
│   └── index-[hash].js    # ملف JavaScript المضغوط
└── [جميع الصور]           # الصور من مجلد public
```

---

## 🌐 خيارات النشر

### خيار 1: رفع على CWP/cPanel

```bash
# 1. بناء المشروع
npm run build

# 2. ضغط مجلد dist
# على Windows:
Compress-Archive -Path .\dist\* -DestinationPath dist.zip

# 3. رفع dist.zip للسيرفر
# 4. فك الضغط في public_html
# 5. انسخ ملف .htaccess إلى public_html
```

### خيار 2: رفع على GitHub Pages

```bash
# 1. بناء ونشر
npm run build
npm run deploy
```

### خيار 3: رفع على Netlify

```bash
# 1. بناء المشروع
npm run build

# 2. اذهب إلى netlify.com
# 3. اسحب مجلد dist
```

### خيار 4: رفع على Vercel

```bash
# تثبيت Vercel CLI
npm i -g vercel

# نشر المشروع
vercel --prod
```

---

## 🔧 إعدادات خاصة

### للنشر على مجلد فرعي (subdirectory):

عدّل `vite.config.js`:

```javascript
base: command === "build" ? "/subfolder/" : "/",
```

### للنشر على نطاق رئيسي (root domain):

عدّل `vite.config.js`:

```javascript
base: command === "build" ? "/" : "/",
```

---

## ✅ التحقق بعد النشر

افحص هذه النقاط:

- [ ] الصفحة الرئيسية تفتح
- [ ] جميع الصور تظهر
- [ ] التبديل بين اللغات يعمل
- [ ] الروابط الخارجية تعمل
- [ ] وسائل التواصل تفتح الحسابات الصحيحة
- [ ] الموقع سريع ويعمل على الموبايل

---

## 📝 ملاحظات مهمة

### الملفات المطلوبة على السيرفر:

- ✅ `index.html`
- ✅ مجلد `assets/`
- ✅ جميع الصور (_.jpg, _.png)
- ✅ `.htaccess` (للسيرفرات Apache)

### ملفات لا يجب رفعها:

- ❌ `node_modules/`
- ❌ `src/`
- ❌ `package.json`
- ❌ `.git/`
- ❌ `.env`

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الصفحة بيضاء فارغة

**الحل:**

- تحقق من console في المتصفح (F12)
- تأكد من مسار `base` صحيح في `vite.config.js`
- تأكد من رفع جميع الملفات

### المشكلة: الصور لا تظهر

**الحل:**

- تأكد من رفع جميع الصور من مجلد `public`
- تحقق من الأذونات (644 للملفات)
- افحص مسارات الصور في console

### المشكلة: خطأ 404

**الحل:**

- تأكد من وجود `.htaccess`
- تحقق من تفعيل mod_rewrite
- تأكد من مسار `base` في الإعدادات

---

## 📊 الأداء

### تحسين السرعة:

1. ✅ الكود مضغوط تلقائياً (minified)
2. ✅ الصور مع lazy loading
3. ✅ CSS و JS محسّنة
4. ✅ Browser caching مفعّل

### النتيجة المتوقعة:

- **سرعة التحميل:** < 2 ثانية
- **حجم الصفحة:** ~ 500 KB
- **درجة الأداء:** 90+ على Google PageSpeed

---

## 🔄 التحديثات المستقبلية

```bash
# 1. عدّل الكود
# 2. بناء جديد
npm run build

# 3. ارفع الملفات الجديدة
# (استبدل الملفات القديمة)

# 4. امسح الكاش
# Ctrl + Shift + R في المتصفح
```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. راجع ملف `CWP-DEPLOYMENT.md` للتفاصيل
2. افحص console في المتصفح (F12)
3. تحقق من error logs في السيرفر

---

**بالتوفيق! 🎉**



